PyYAML>=6.0
pytest>=7.0
maturin>=1.0
streamlit>=1.28
# Optional for autonomous online exploration (DuckDuckGo, no API key):
requests>=2.28
beautifulsoup4>=4.11
duckduckgo-search>=4.0
# Optional for graph export (step 6): pip install graphviz (and Graphviz binaries from https://graphviz.org)
# Heavy optional: matplotlib (dashboard/viz). Use only if needed.
networkx>=3.0
# Optional: pip install matplotlib
# Optional (embeddings): pip install sentence-transformers
sentence-transformers>=2.2
# Optional (vector search): pip install sqlite-vss
sqlite-vec>=0.1.0
PyPDF2>=3.0
